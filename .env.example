# .env.example - 环境变量示例文件
# 将此文件复制为 .env 并填入你的实际值。

# 你的 Discord 机器人令牌
DISCORD_TOKEN="YOUR_DISCORD_TOKEN_HERE"

# (可选) 用于在开发时快速同步斜杠命令的服务器（公会）ID
# 如果留空，命令将进行全局同步（可能需要长达一小时才能生效）
GUILD_ID="YOUR_DEVELOPMENT_GUILD_ID_HERE"

# --- 新增：权限控制 ---
# 拥有此命令访问权限的开发者用户ID，多个ID请用逗号分隔
DEVELOPER_USER_IDS="YOUR_USER_ID_1,YOUR_USER_ID_2"

# 拥有此命令访问权限的身份组ID，多个ID请用逗号分隔
ADMIN_ROLE_IDS="YOUR_ROLE_ID_1,YOUR_ROLE_ID_2"

# Google Gemini API 密钥 (用于AI对话功能)
# 多个密钥请直接换行，并用英文双引号 "..." 将所有密钥包裹起来
GOOGLE_API_KEYS_LIST="
YOUR_GEMINI_API_KEY_1
YOUR_GEMINI_API_KEY_2
"

# --- 功能开关 ---
# 是否启用聊天功能 (True/False)
# 这是一个全局紧急开关，可在不修改数据库的情况下快速禁用整个聊天功能
CHAT_ENABLED=True

# 是否记录AI完整的上下文日志 (true/false)
# 用于调试，会记录发送给AI的完整上下文
LOG_AI_FULL_CONTEXT=true

# --- 工具加载器配置 ---
# 禁用的工具模块列表（文件名，不含.py扩展名），多个用逗号分隔
DISABLED_TOOLS="get_yearly_summary"

# --- Gradio TTS 配置 ---
# 新版 TTS 工具使用的 Gradio / Ngrok 地址（不要硬编码在代码里）
GRADIO_API_URL="https://your-gradio-or-ngrok-url.ngrok-free.dev"

# --- 联网搜索工具配置 ---
# 联网搜索工具使用的 Vercel AI Gateway key
SEARCH_API_KEY=""

# --- Pixiv 工具配置 ---
PIXIV_REFRESH_TOKEN=""
PIXIV_PROXY=""
PIXIV_API_PROXY_HOST=""
PIXIV_IMAGE_PROXY_HOST="i.pixiv.re"
PIXIV_USE_IMAGE_PROXY="true"
PIXIV_R18_DEFAULT_MODE="safe"
PIXIV_ALLOW_AI_DEFAULT="false"
PIXIV_DEFAULT_EXCLUDED_TAGS="ntr,futa,guro,scat,amputee,bestiality,vomit"

# --- 类脑币系统 ---
# 在指定服务器发帖可获得奖励的服务器ID，多个用逗号隔开
COIN_REWARD_GUILD_IDS="YOUR_GUILD_ID_1,YOUR_GUILD_ID_2"

# --- Discord OAuth 配置 ---
# 用于Web界面的Discord OAuth认证
VITE_DISCORD_CLIENT_ID="YOUR_DISCORD_CLIENT_ID"
DISCORD_CLIENT_SECRET="YOUR_DISCORD_CLIENT_SECRET"

# --- WebUI 配置 ---
# 是否启用 WebUI 心跳、日志上报与配置面板
ENABLE_WEBUI=true
# WebUI 管理员登录口令，支持多行；每行一个 token
WEBUI_ADMIN_TOKEN="
YOUR_WEBUI_ADMIN_TOKEN
"
# 本机映射出的 WebUI 访问端口
CONFIG_WEB_PORT=8080
# Bot 向 WebUI 回传心跳和日志时使用的地址
WEBUI_LOG_SERVER_URL="http://config_web:80/api/log"

# --- Docker 镜像部署配置 ---
# 直接拉取镜像部署时使用的应用镜像地址
# 例如：docker.io/your-dockerhub-username/odysseia-guidance:latest
APP_IMAGE="docker.io/your-dockerhub-username/odysseia-guidance:latest"

# --- Gemini 调试配置 ---
# 是否记录详细的Gemini处理过程
LOG_DETAILED_GEMINI_PROCESS=True

# --- AI Gateway 生图配置 ---
IMAGINE_API_KEY=""    # 填写 Vercel AI Gateway key。绘图面板内可切换 Gemini / Grok 模型

# --- 论坛帖子语义搜索 ---
# 需要进行RAG索引的论坛频道ID，多个用逗号隔开
# 这些频道的帖子会被索引到向量数据库，用于语义搜索
FORUM_SEARCH_CHANNEL_IDS="YOUR_FORUM_CHANNEL_ID_1,YOUR_FORUM_CHANNEL_ID_2"

# --- 自定义 Gemini 端点配置 ---
# (可选) 自定义 Gemini API 端点，例如公益站
CUSTOM_GEMINI_URL=""
CUSTOM_GEMINI_API_KEY=""

SUMMARY_MODEL='deepseek-chat'  # 记忆总结模型

# --- PostgreSQL 数据库配置 ---
POSTGRES_DB="your_database_name"
POSTGRES_USER="your_username"
POSTGRES_PASSWORD="your_password"
DB_PORT=5432

# --- 备份输出目录（可选）---
# 留空：仅备份到项目内 src/backup
# 填写：备份后会额外复制到该目录（可填项目内或宿主机绝对路径，如 /srv/odysseia-backup/auto_backup）
BACKUP_OUTPUT_DIR=""

VPS_MODE=false  # 为小宽带vps设计。强制将图片压缩到1mb以下，加快回复速度


# --- 自定义 Openai 端点配置 ---
DEEPSEEK_URL="https://api.deepseek.com/"
DEEPSEEK_API_KEY=""
MOONSHOT_URL="https://api.moonshot.cn/v1"
MOONSHOT_API_KEY=""   #支持多个key轮换

CUSTOM_MODEL_URL=''   #支持自定义openai格式模型，可在聊天设置中热切换
CUSTOM_MODEL_API_KEY=''   # 可直接填写 key，或填写 /data/CUSTOM_MODEL_API_KEY.json（会自动映射到 /app/data）
# 文件模式 JSON 格式：{"api_keys": ["vck_xxx", "vck_yyy"]}
CUSTOM_MODEL_NAME=''
CUSTOM_MODEL_ENABLE_VISION='true'  # 启动custom外置识图功能(模型自身不能读图时开启)需要配置MOONSHOT_API_KEY
CUSTOM_MODEL_ENABLE_VIDEO_INPUT='false'

# --- kimi 公益站端点配置 ---
CUSTOM_MOONSHOT_URL=""
CUSTOM_MOONSHOT_API_KEY=""
CUSTOM_MOONSHOT_MODEL_NAME="" #公益站自定义模型名
IS_NV_MODEL=True   #大多公益站使用nvidia的模型，需要特殊的关闭思考模式的方式

# --- GPT Image 2 投喂生图 ---
GPT_IMAGE_API_KEY="sk-xxx"
GPT_IMAGE_BASE_URL="https://api.openai.com/v1"
GPT_IMAGE_MODEL="gpt-image-2"
